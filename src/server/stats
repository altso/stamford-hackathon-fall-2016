var data = storage.global["timetableData"];
return {
  "stops": data.stops ? data.stops.length : 0,
  "stop_times": data.stop_times ? data.stop_times.length : 0,
  "trips": data.trips ? data.trips.length : 0,
  "routes": data.routes ? data.routes.length : 0
}